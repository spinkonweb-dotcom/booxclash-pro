fastapi
uvicorn[standard]
pydantic
python-dotenv
requests
websockets
# The critical package for your error:
google-generativeai
# The newer Google SDK (keep if you are experimenting with it, otherwise optional):
google-genai
# Vector database:
pinecone
httpx
firebase_admin
